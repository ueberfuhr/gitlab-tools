{
  "name": "@ars/gitlab-tools",
  "version": "1.0.0",
  "description": "GitLab Tools",
  "directories": {
    "doc": "doc"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "rm -rf dist && tsc && cp -r src/public/. dist",
    "copy-env": "copy-partial-json -s http-requests/http-client.private.env.json  -t dist/env.json -k dev.token sample-project-id",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.ars.de:ars/schulungen/tools/gitlab.git"
  },
  "keywords": [
    "gitlab",
    "tools"
  ],
  "author": "Ralf Ueberfuhr (ARS)",
  "license": "ISC",
  "devDependencies": {
    "copy-partial-json": "^1.1.4",
    "typescript": "^4.6.4"
  }
}
